## 1. Contract Promotion

- [x] 1.1 Verify the live gameplay recovery collection contract is deterministic and uses only permitted handwritten boundary fakes.
- [x] 1.2 Add the contract to both maintained backend workflow selections in identical order.

## 2. Verification

- [x] 2.1 Validate the OpenSpec change, static constraints, direct suite, and workflow selection parity.
- [x] 2.2 Run the complete maintained backend suite at the current threshold and keep the 47% threshold because the 47.82% result does not warrant a strict 48% candidate.
