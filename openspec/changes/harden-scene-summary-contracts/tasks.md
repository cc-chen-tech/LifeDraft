## 1. Deterministic State Contracts

- [x] 1.1 Add a new `PlayerState`-backed historical-summary test file covering
  configured-character, commitment, and active-foreshadowing keyword sources.
- [x] 1.2 Add current/future summary exclusion and recency-scoring scenarios
  without random patches, provider calls, or mocks.

## 2. Stable Gate Promotion

- [x] 2.1 Run the new summary suite and the existing multi-character scene
  suite twice in the maintained environment.
- [x] 2.2 Add only those stable suites to both maintained workflow lists in
  identical order.
- [x] 2.3 Measure the expanded maintained selection twice and raise the floor
  only when both results prove the next integer.

## 3. Verification and Record

- [x] 3.1 Validate OpenSpec, workflow parity, source coverage delta, and the
  test-only diff.
- [x] 3.2 Commit the OpenSpec artifacts, new tests, and workflow updates.
