## ADDED Requirements

### Requirement: Parser gate selection is symmetric
The maintained workflow selections SHALL list parser contracts in identical order.

#### Scenario: Lists are compared
- **WHEN** selections are extracted
- **THEN** ordered paths MUST match
