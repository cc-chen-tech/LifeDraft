## 1. Narrative State Contracts

- [x] 1.1 Add real-`PlayerState` storyline and fact-update contracts for
  replacement, resolution, expiry, and bounded retention.
- [x] 1.2 Add real-`PlayerState` foreshadowing and habit-update contracts for
  metadata normalization, cleanup, priority ordering, and per-domain limits.

## 2. Maintained Gate Promotion

- [x] 2.1 Run the new narrative-state contract suite twice with the maintained
  environment variables.
- [x] 2.2 Add the twice-stable suite to both maintained backend workflows in
  matching order.
- [x] 2.3 Measure the expanded maintained selection twice and raise the floor
  only when both results support the next integer.

## 3. Verification and Record

- [x] 3.1 Validate OpenSpec, workflow parity, no-mock constraints, source
  coverage delta, and test-only diff.
- [x] 3.2 Commit the OpenSpec artifacts, new tests, and workflow updates.
