## Context

RoundEventGenerator has deterministic fallbacks independent of model execution.

## Goals / Non-Goals

**Goals:** gate bilingual scheduled-event fallback content and nested settings extraction.

**Non-Goals:** invoke generation or modify production behavior.
