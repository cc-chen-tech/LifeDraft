## 1. Choice State Contracts

- [x] 1.1 Add mock-free tests for resource-bound normalization and its warning metadata.
- [x] 1.2 Add mock-free tests for idempotent choice wealth transactions and invalid deltas.
- [x] 1.3 Run the new contract file twice with the maintained test environment.

## 2. Gate Promotion and Verification

- [x] 2.1 Add the verified file to both maintained workflow selections in identical order.
- [x] 2.2 Run the complete maintained coverage command twice and raise the floor only when supported by both measurements.
- [x] 2.3 Validate OpenSpec, workflow parity, static gate constraints, and diff hygiene.
- [x] 2.4 Commit the test-only change without pushing or opening a pull request.
