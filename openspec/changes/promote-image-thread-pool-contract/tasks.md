## 1. Contract Promotion

- [x] 1.1 Verify the image thread-pool lifecycle contract is deterministic and contains no forbidden maintained-gate constructs.
- [x] 1.2 Add the contract to both maintained backend workflow selections in identical order.

## 2. Verification

- [x] 2.1 Validate the OpenSpec change, workflow selection parity, and direct contract run.
- [x] 2.2 Run the full maintained backend suite at the current threshold and test the next threshold only if the result warrants it.
