# 阿里云完整部署指南

本指南将手把手教你如何在阿里云上部署"人生草稿本"应用,并配置自定义域名访问。

---

## 📋 目录

1. [准备工作](#准备工作)
2. [购买阿里云ECS服务器](#购买阿里云ecs服务器)
3. [购买和配置域名](#购买和配置域名)
4. [服务器环境配置](#服务器环境配置)
5. [部署应用](#部署应用)
6. [配置Nginx和SSL](#配置nginx和ssl)
7. [域名解析与访问](#域名解析与访问)
8. [常见问题](#常见问题)

---

## 准备工作

### 需要准备的内容：

1. **阿里云账号**
   - 访问 https://www.aliyun.com 注册账号
   - 完成实名认证（个人或企业）
   - 准备好支付方式

2. **本地环境**
   - 确保代码已经推送到Git仓库（GitHub/Gitee等）
   - 准备好`.env`配置文件
   - 记录下你的OpenAI API Key

3. **域名准备**（可选，但强烈推荐）
   - 可以在阿里云购买域名
   - 或者使用已有域名转入阿里云DNS解析

---

## 购买阿里云ECS服务器

### 步骤1：选择服务器配置

1. **登录阿里云控制台**
   - 访问 https://ecs-buy.aliyun.com/

2. **推荐配置**（新用户可选特惠套餐）：
   ```
   地域：华东2（上海）或你用户所在地就近区域
   实例规格：
     - 最低配置：2核4GB（适合测试/小规模使用）
     - 推荐配置：2核8GB 或 4核8GB（适合生产环境）
   
   操作系统：Ubuntu 22.04 LTS 64位（推荐）
              或 CentOS 7.9 64位
   
   存储：40GB系统盘（云效率盘）
   
   带宽：按固定带宽1-5Mbps
          或按使用流量计费（推荐，成本更低）
   
   购买时长：建议1年起（有优惠）
   ```

3. **网络与安全组配置**
   - 选择专有网络VPC（默认即可）
   - 安全组规则：选择"暂不设置"（稍后手动配置）

4. **设置实例信息**
   - 实例名称：`story2-server`
   - 登录凭证：选择"自定义密码"，设置root密码（务必记住！）

5. **确认订单并支付**

### 步骤2：配置安全组规则

购买完成后，需要开放必要的端口：

1. **进入ECS控制台**
   - 找到你的实例 → 点击"安全组"

2. **添加安全组规则**（入方向）：
   ```
   规则1 - SSH访问
   协议：自定义TCP
   端口：22
   授权对象：0.0.0.0/0
   描述：SSH远程登录
   
   规则2 - HTTP访问
   协议：HTTP(80)
   端口：80
   授权对象：0.0.0.0/0
   描述：网站HTTP访问
   
   规则3 - HTTPS访问
   协议：HTTPS(443)
   端口：443
   授权对象：0.0.0.0/0
   描述：网站HTTPS访问
   
   规则4 - 应用端口（可选，调试用）
   协议：自定义TCP
   端口：8501
   授权对象：0.0.0.0/0
   描述：Streamlit应用直接访问（调试后可关闭）
   ```

3. **获取服务器公网IP**
   - 在ECS实例列表中，复制你的"公网IP地址"
   - 例如：`123.456.789.10`

---

## 购买和配置域名

### 选项A：在阿里云购买新域名

1. **进入域名注册页面**
   - 访问 https://wanwang.aliyun.com/domain/

2. **搜索并购买域名**
   - 输入你想要的域名，如：`mystory2.com`
   - 选择后缀（.com/.cn/.top 等）
   - 加入购物车并支付

3. **域名实名认证**
   - 购买后需要实名认证（3-5个工作日）
   - 上传身份证/营业执照
   - 填写真实联系信息

### 选项B：使用已有域名

1. **转入域名到阿里云**（可选）
   - 在原注册商获取转移密码
   - 在阿里云提交域名转入申请

2. **使用阿里云DNS解析**
   - 在原注册商修改域名DNS服务器为阿里云DNS：
     ```
     ns1.alidns.com
     ns2.alidns.com
     ```

### 域名备案（重要！）

**如果你的服务器在中国大陆，域名必须备案才能访问！**

1. **进入ICP备案系统**
   - 阿里云控制台 → ICP备案

2. **准备备案材料**
   - 个人：身份证、手机号、邮箱
   - 企业：营业执照、法人身份证

3. **提交备案申请**
   - 按照系统提示填写信息
   - 拍摄核验照片（使用阿里云APP）
   - 等待审核（通常5-20个工作日）

**临时方案**：如果不想备案，可以：
- 购买香港/海外ECS（无需备案，但延迟稍高）
- 或者暂时使用IP地址访问

---

## 服务器环境配置

### 步骤1：连接到服务器

**使用SSH连接**：

```bash
# macOS/Linux直接使用终端
ssh root@你的服务器IP

# Windows使用PowerShell或安装Git Bash
ssh root@你的服务器IP

# 输入刚才设置的root密码
```

### 步骤2：更新系统

```bash
# Ubuntu系统
sudo apt update && sudo apt upgrade -y

# CentOS系统
# sudo yum update -y
```

### 步骤3：安装Docker

```bash
# 下载Docker安装脚本
curl -fsSL https://get.docker.com -o get-docker.sh

# 执行安装
sh get-docker.sh

# 启动Docker服务
systemctl start docker
systemctl enable docker

# 验证安装
docker --version
```

### 步骤4：安装Docker Compose

```bash
# 安装Docker Compose插件
sudo apt-get install docker-compose-plugin -y

# 验证安装
docker compose version
```

### 步骤5：安装Git和其他工具

```bash
# 安装必要工具
sudo apt-get install -y git vim curl wget

# 配置Git（用于拉取代码）
git config --global user.name "Your Name"
git config --global user.email "your@email.com"
```

---

## 部署应用

### 步骤1：拉取代码

```bash
# 进入工作目录
cd /home

# 克隆你的代码仓库（替换为你的仓库地址）
git clone https://github.com/yourusername/story2.git

# 进入项目目录
cd story2
```

**如果是私有仓库**：
```bash
# 使用Personal Access Token
git clone https://用户名:token@github.com/yourusername/story2.git

# 或使用SSH密钥（推荐）
ssh-keygen -t rsa -b 4096 -C "your@email.com"
cat ~/.ssh/id_rsa.pub  # 复制公钥到GitHub Settings -> SSH Keys
git clone git@github.com:yourusername/story2.git
```

### 步骤2：配置环境变量

```bash
# 创建.env文件
cp .env.example .env

# 编辑配置文件
vim .env
```

在vim中按`i`进入编辑模式，填入以下内容：

```bash
# OpenAI配置
OPENAI_API_KEY=sk-your-actual-api-key-here
OPENAI_MODEL=gpt-4
OPENAI_BASE_URL=

# 数据库配置（使用默认SQLite）
DATABASE_URL=

# 应用配置
DEFAULT_LANGUAGE=zh
CACHE_EVENTS=true

# 生产环境标识
ENVIRONMENT=production
```

按`Esc`退出编辑模式，输入`:wq`保存并退出。

### 步骤3：构建和启动应用

```bash
# 构建Docker镜像（首次较慢，约5-10分钟）
docker compose build

# 启动应用（开发环境，用于测试）
docker compose up -d

# 查看日志，确认启动成功
docker compose logs -f app
```

看到类似输出说明启动成功：
```
You can now view your Streamlit app in your browser.
URL: http://0.0.0.0:8501
```

按`Ctrl+C`退出日志查看。

### 步骤4：测试访问

在浏览器中访问：
```
http://你的服务器IP:8501
```

如果能看到应用界面，说明部署成功！

---

## 配置Nginx和SSL

现在我们要配置Nginx反向代理和HTTPS，让应用可以通过域名访问。

### 步骤1：创建Nginx配置文件

```bash
# 确保在项目目录下
cd /home/story2

# 创建Nginx配置文件
vim nginx.conf
```

粘贴以下内容（**替换your-domain.com为你的域名**）：

```nginx
events {
    worker_connections 1024;
}

http {
    upstream streamlit {
        server app:8501;
    }

    server {
        listen 80;
        server_name your-domain.com www.your-domain.com;

        # 临时：HTTP访问（用于申请SSL证书）
        location / {
            proxy_pass http://streamlit;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # Streamlit特殊配置
            proxy_buffering off;
            proxy_cache off;
        }

        # 健康检查端点
        location /health {
            access_log off;
            return 200 "healthy\n";
        }
    }

    # HTTPS配置（在获取SSL证书后取消注释）
    # server {
    #     listen 443 ssl http2;
    #     server_name your-domain.com www.your-domain.com;
    #
    #     ssl_certificate /etc/nginx/ssl/fullchain.pem;
    #     ssl_certificate_key /etc/nginx/ssl/privkey.pem;
    #     
    #     ssl_protocols TLSv1.2 TLSv1.3;
    #     ssl_ciphers HIGH:!aNULL:!MD5;
    #     ssl_prefer_server_ciphers on;
    #
    #     location / {
    #         proxy_pass http://streamlit;
    #         proxy_http_version 1.1;
    #         proxy_set_header Upgrade $http_upgrade;
    #         proxy_set_header Connection "upgrade";
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #         
    #         proxy_buffering off;
    #         proxy_cache off;
    #     }
    #
    #     location /health {
    #         access_log off;
    #         return 200 "healthy\n";
    #     }
    # }
    #
    # # HTTP重定向到HTTPS
    # server {
    #     listen 80;
    #     server_name your-domain.com www.your-domain.com;
    #     return 301 https://$server_name$request_uri;
    # }
}
```

### 步骤2：创建SSL证书目录

```bash
# 创建SSL证书目录
mkdir -p nginx/ssl
```

### 步骤3：使用生产环境配置启动

```bash
# 停止之前的容器
docker compose down

# 使用生产环境配置启动（包含Nginx）
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d

# 查看所有容器状态
docker compose ps
```

你应该看到两个容器正在运行：
- `story2-app` - 应用容器
- `story2-nginx` - Nginx容器

### 步骤4：申请免费SSL证书（Let's Encrypt）

**方法A：使用certbot自动申请（推荐）**

```bash
# 安装certbot
sudo apt-get install certbot -y

# 停止Nginx容器（避免端口冲突）
docker stop story2-nginx

# 申请证书（替换为你的域名和邮箱）
sudo certbot certonly --standalone \
  -d your-domain.com \
  -d www.your-domain.com \
  --email your@email.com \
  --agree-tos \
  --non-interactive

# 复制证书到项目目录
sudo cp /etc/letsencrypt/live/your-domain.com/fullchain.pem nginx/ssl/
sudo cp /etc/letsencrypt/live/your-domain.com/privkey.pem nginx/ssl/

# 修改权限
sudo chmod 644 nginx/ssl/*.pem

# 启动Nginx
docker start story2-nginx
```

**方法B：使用阿里云免费SSL证书**

1. 进入阿里云SSL证书控制台
2. 选择"免费证书"→"立即购买"（实际免费）
3. 购买后，点击"证书申请"
4. 填写域名信息，选择DNS验证
5. 按照提示添加DNS解析记录
6. 等待证书签发（通常几分钟）
7. 下载证书，选择"Nginx"格式
8. 上传到服务器：

```bash
# 在本地解压下载的证书包，有两个文件：
# - your-domain.com.pem
# - your-domain.com.key

# 上传到服务器（在本地终端执行）
scp your-domain.com.pem root@服务器IP:/home/story2/nginx/ssl/fullchain.pem
scp your-domain.com.key root@服务器IP:/home/story2/nginx/ssl/privkey.pem
```

### 步骤5：启用HTTPS配置

```bash
# 编辑nginx.conf
vim nginx.conf

# 删除或注释掉临时HTTP配置
# 取消注释HTTPS配置部分
# 确保域名替换正确
```

**完整的生产环境配置**（修改后的nginx.conf）：

```nginx
events {
    worker_connections 1024;
}

http {
    upstream streamlit {
        server app:8501;
    }

    # HTTPS服务器
    server {
        listen 443 ssl http2;
        server_name your-domain.com www.your-domain.com;

        ssl_certificate /etc/nginx/ssl/fullchain.pem;
        ssl_certificate_key /etc/nginx/ssl/privkey.pem;
        
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;
        ssl_prefer_server_ciphers on;
        ssl_session_cache shared:SSL:10m;
        ssl_session_timeout 10m;

        location / {
            proxy_pass http://streamlit;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            proxy_buffering off;
            proxy_cache off;
            proxy_read_timeout 86400;
        }

        location /health {
            access_log off;
            return 200 "healthy\n";
        }
    }

    # HTTP重定向到HTTPS
    server {
        listen 80;
        server_name your-domain.com www.your-domain.com;
        return 301 https://$server_name$request_uri;
    }
}
```

### 步骤6：重启Nginx

```bash
# 重启Nginx容器以加载新配置
docker restart story2-nginx

# 检查Nginx日志
docker logs story2-nginx
```

---

## 域名解析与访问

### 步骤1：配置DNS解析

1. **进入阿里云DNS控制台**
   - 访问 https://dns.console.aliyun.com/

2. **添加解析记录**：

   **记录1 - 主域名**
   ```
   记录类型：A
   主机记录：@
   解析线路：默认
   记录值：你的服务器公网IP（如 123.456.789.10）
   TTL：10分钟
   ```

   **记录2 - www子域名**
   ```
   记录类型：A
   主机记录：www
   解析线路：默认
   记录值：你的服务器公网IP
   TTL：10分钟
   ```

3. **等待DNS生效**
   - 通常1-10分钟
   - 可以使用以下命令检查：

```bash
# 检查DNS解析
nslookup your-domain.com
ping your-domain.com
```

### 步骤2：访问你的应用

在浏览器中访问：

```
https://your-domain.com
```

🎉 **恭喜！你的应用已成功部署！**

---

## 常见问题

### Q1: 域名访问提示"无法访问此网站"

**排查步骤**：
1. 检查DNS是否生效：`nslookup your-domain.com`
2. 检查服务器防火墙：`sudo ufw status`（如果启用了）
3. 检查安全组规则：确保80和443端口已开放
4. 检查Nginx容器状态：`docker ps`
5. 查看Nginx日志：`docker logs story2-nginx`

### Q2: HTTPS证书错误

**解决方法**：
- 确认证书文件路径正确
- 检查证书是否过期：`openssl x509 -in nginx/ssl/fullchain.pem -noout -dates`
- 重新申请证书

### Q3: 应用更新如何部署？

```bash
# 1. 进入项目目录
cd /home/story2

# 2. 拉取最新代码
git pull

# 3. 重建镜像
docker compose build

# 4. 重启服务（使用生产配置）
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d

# 5. 查看日志确认
docker compose logs -f app
```

### Q4: 如何备份数据？

```bash
# 备份数据目录
tar -czf backup-$(date +%Y%m%d).tar.gz /home/story2/data/

# 下载到本地（在本地终端执行）
scp root@服务器IP:/home/story2/backup-20240211.tar.gz ./
```

### Q5: 证书自动续期

Let's Encrypt证书有效期90天，需要定期续期：

```bash
# 添加定时任务
sudo crontab -e

# 添加以下行（每月1号凌晨3点自动续期）
0 3 1 * * certbot renew --quiet --post-hook "docker restart story2-nginx"
```

### Q6: 性能优化建议

1. **启用CDN加速**（可选）
   - 使用阿里云CDN
   - 配置CNAME到CDN域名
   - 加速静态资源访问

2. **使用PostgreSQL替代SQLite**
   ```bash
   # 取消docker-compose.yml中PostgreSQL服务的注释
   vim docker-compose.yml
   
   # 修改.env添加数据库URL
   DATABASE_URL=postgresql://story2:password@postgres:5432/story2
   
   # 重启服务
   docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
   ```

3. **调整资源限制**
   - 根据实际使用情况修改`docker-compose.yml`中的资源限制

### Q7: 监控与告警

**推荐工具**：
- 阿里云云监控（免费）
- UptimeRobot（免费在线监控）
- Grafana + Prometheus（高级）

---

## 安全建议

1. ✅ **修改SSH默认端口**
   ```bash
   sudo vim /etc/ssh/sshd_config
   # 修改 Port 22 为其他端口，如 Port 2222
   sudo systemctl restart sshd
   ```

2. ✅ **禁用root密码登录，使用SSH密钥**
   ```bash
   # 生成密钥对（在本地执行）
   ssh-keygen -t rsa -b 4096
   
   # 上传公钥到服务器
   ssh-copy-id -i ~/.ssh/id_rsa.pub root@服务器IP
   
   # 禁用密码登录
   sudo vim /etc/ssh/sshd_config
   # 设置 PasswordAuthentication no
   sudo systemctl restart sshd
   ```

3. ✅ **启用防火墙**
   ```bash
   # 安装ufw
   sudo apt-get install ufw
   
   # 允许SSH（注意端口号）
   sudo ufw allow 2222/tcp
   
   # 允许HTTP/HTTPS
   sudo ufw allow 80/tcp
   sudo ufw allow 443/tcp
   
   # 启用防火墙
   sudo ufw enable
   ```

4. ✅ **定期更新系统和Docker**
   ```bash
   # 每月执行一次
   sudo apt update && sudo apt upgrade -y
   docker system prune -a --volumes -f
   ```

5. ✅ **保护.env文件**
   ```bash
   chmod 600 .env
   ```

---

## 成本预估

以阿里云为例（按年付费）：

| 项目 | 配置 | 价格（约） |
|------|------|-----------|
| ECS服务器 | 2核4GB，1Mbps带宽 | ¥300-800/年 |
| 域名 | .com/.cn | ¥50-100/年 |
| SSL证书 | Let's Encrypt | 免费 |
| ICP备案 | 首次备案 | 免费 |
| **总计** | | **¥350-900/年** |

**新用户优惠**：阿里云新用户通常有首购折扣，可能低至¥100-300/年。

---

## 总结

至此，你已经完成了：

- ✅ 购买和配置阿里云ECS服务器
- ✅ 注册和备案域名
- ✅ 部署Docker化的应用
- ✅ 配置Nginx反向代理
- ✅ 启用HTTPS加密
- ✅ 配置DNS解析
- ✅ 通过域名访问应用

**下一步建议**：
1. 配置监控告警
2. 设置自动备份
3. 优化性能和安全
4. 收集用户反馈

祝你使用愉快！🎉

---

## 需要帮助？

- 阿里云帮助中心：https://help.aliyun.com/
- Docker官方文档：https://docs.docker.com/
- Let's Encrypt文档：https://letsencrypt.org/zh-cn/docs/

如有问题，欢迎查看主文档 [DEPLOYMENT.md](./DEPLOYMENT.md) 或提交Issue。
