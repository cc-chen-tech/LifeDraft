# 修复图片生成"出戏"问题

## 问题背景

QA 团队实际查看了服务器生成的图片，发现了严重的"出戏"问题。故事设定是**2024年中国现实主义题材**，但生成的图片中出现了大量**科幻/赛博朋克元素**：

1. **科幻风格错位**（最严重）
   - 角色穿着赛博朋克金属夹克，背景是全息城市
   - 出现飞行汽车、全息投影、全息屏幕
   - 人物眼睛发红光

2. **人物完全不一致**（严重）
   - 同一个"李逍遥"两张肖像完全是不同的人
   - 脸型、体型、气质全不一样

3. **品牌植入**（中等）
   - 星巴克Logo清晰可见

## 需要修改的文件

1. `config/prompts/_helpers.py` - `_build_image_era_constraints()` 函数
   - 当前现代背景只返回轻量级约束，缺少**反科幻约束**
   - 需要添加：禁止全息投影、禁止赛博朋克服装、禁止发光眼睛、禁止品牌植入等

2. `src/services/image/scene_service.py` - `SCENE_PROMPT_TEMPLATE` 和场景生成逻辑
   - 场景模板缺少现实主义风格约束
   - 需要在模板中加入：写实风格、禁止科幻元素、禁止品牌植入等

3. `src/services/image/character_service.py` - 角色肖像生成逻辑
   - 缺少人物一致性约束（同角色不同图片应该看起来像同一个人）
   - 缺少服装现实主义约束（2024年普通人穿什么）
   - 需要在 prompt 中加入写实摄影风格约束

4. `src/services/image/style_manager.py` - 颜色调板
   - 检查是否有科幻倾向的描述词（如"梦幻光晕"、"全息"等）
   - 为现代现实主义场景添加专门的调板

5. `src/ai/image_prompt_builder.py` - prompt 构建器
   - 检查角色 prompt 构建是否有科幻倾向
   - 添加现实主义约束

## 修改原则

- **写实主义优先**：所有图片必须是写实摄影风格，禁止任何科幻、奇幻、超现实元素
- **时代准确**：2024年中国，人物穿日常服装（T恤、衬衫、外套、牛仔裤等），背景是真实城市/街道/室内
- **人物一致性**：同一角色的所有图片必须有相同的外貌特征
- **禁止品牌**：禁止出现真实品牌Logo（星巴克、苹果等）
- **渐进修复**：每次修改后检查是否引入新问题

## 验证要求

修改完成后：
1. 运行现有测试确保不破坏已有功能
2. 检查修改后的 prompt 是否包含所有必要约束
3. 确认约束不会过度限制导致生成失败

## 完成信号

当所有文件都已修改、约束已添加、测试通过时，输出：

```
<promise>IMAGE_PROMPT_FIXES_COMPLETE</promise>
```
