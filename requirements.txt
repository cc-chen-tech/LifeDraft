openai>=2.0.0
httpx==0.27.0
python-dotenv>=1.0.0
pydantic>=2.0.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0  # PostgreSQL驱动(云部署)
pytest>=7.0.0
fastapi>=0.115.0
uvicorn>=0.30.0
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.9

# 向量检索（可选，启用 ENABLE_VECTOR_SEARCH 时需要）
chromadb>=0.4.0

# ========== 开发工具 (代码质量) ==========
# 代码格式化
black>=23.0.0

# Import 排序
isort>=5.12.0

# 代码检查
flake8>=6.0.0

# 类型检查
mypy>=1.0.0

# 安全扫描
bandit>=1.7.0

# Pre-commit hooks
pre-commit>=3.0.0

# 测试覆盖率
pytest-cov>=4.0.0
