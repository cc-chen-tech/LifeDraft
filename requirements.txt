openai>=2.0.0
httpx==0.27.0
requests>=2.31.0
websockets>=16.0
mutagen>=1.47.0
python-dotenv>=1.0.0
# Offline Chinese POS segmentation for conservative world-projection subject binding.
# thulac's bundled model is used locally; extraction never downloads a model at runtime.
thulac>=0.2.2,<0.3
pydantic>=2.0.0
tokenizers>=0.20.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0  # PostgreSQL驱动(云部署)
pytest>=7.0.0
fastapi==0.136.1
uvicorn>=0.30.0

# 缓存库 (M-01 图片生成缓存)
cachetools>=5.0.0

# 图片处理 (图片压缩)
Pillow>=10.0.0

python-jose[cryptography]>=3.3.0
python-multipart>=0.0.9
PyJWT>=2.0.0

# ========== 开发工具 (代码质量) ==========
# 代码格式化
black>=23.0.0

# Import 排序
isort>=5.12.0

# 代码检查
flake8>=6.0.0

# 类型检查
mypy>=1.0.0
typing_extensions>=4.0.0

# 安全扫描
bandit>=1.7.0

# Pre-commit hooks
pre-commit>=3.0.0

# 测试覆盖率
pytest-cov>=4.0.0

# 异步测试支持
pytest-asyncio>=0.23.0

# Sentry 错误监控
sentry-sdk[fastapi]>=2.0.0
