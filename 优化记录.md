 我如果想基于千问的模型在故事中做一些插图，需要保持人物形象的一致性，地点的一致性，以及关键重点物品的一致性，请你给做出实现的技术方案。 

 下面是一些其他的设计的关键要求：
 1. 在第四步人物设定生成完后，增加一步人物形象的生成，需要基于各种背景设置生成人物形象，并且支持用户通过文字修改重新生成人物形象。 在人物生成的同时，在后台默默的完成其他的后续第5到第8步的生成，不再做前端的展示。
 2. 在一些关键人物的生成时也生成人物形象（暂时不支持用户修改），如果其他的人物形象还没有生成出来，不要作为后续流程的卡点，可以等待后台图像异步生成。 同时故事过程中的人物加入也需要人物形象的生成，也是异步生成，不要影响现有的流程。
 3. 除了人物形象外，还有一些重要的物品，地标等也需要提前生成，也是异步生成。是否是重要物品，以及是否需要生成，都由deepseek的模型来判断。
 4. 每一轮故事生成完后，首先将这一轮的全部故事喂给deepseek的模型，判断哪个是最关键的1～2个场景用于图片的生成，然后生成对于这个场景的描述，以及从系统里选出有关的人物，物件，地标等图片用于这个场景的插图生成。


在不同的文生图步骤应该是需要不同的模型，各种需要的配置url和apikey等，你可以写好需要的配置名称，我来填写。

因为这个是一个比较复杂的工程改造，应该有一些需要我二次明确的地方，请你给出具体的问题，几个可选的方案，以及方案的优点和缺点。并且不要一次直接生成全部的改造，而是一步步实现，从最简单的实现和最小的改动开始，分成至少3期的实现方式来实现。 


1. 给人物的形象生成那里加一个重新生成的按钮，这个就是抛弃掉各种用户历史的修改意见的文本，重新用qwen-image去生成。 
2. 先讲已经生成好的各种人物的信息喂给deepseek，然后让deepseek的模型去生成一个图片生成的prompt，再将其喂给文生图的模型去做图片的生成。


在开始我的人生这里，增加一个插画。 
1. 整体插画在最后生成，需要将故事文本喂给deepseek的模型，让其从故事中选一个重要的场景去生成。
2. 需要让deepseek的模型基于故事生成提示词+人物的图片喂给千问的image-edit模型，让其生成插画。
3. 插画生成中也要有一个提示。


方案 A：让 regenerate 也走完整的 generate_round_event 流程

并且检查一下系统里面其他各种重新生成和正常生成的流程有什么不同。 