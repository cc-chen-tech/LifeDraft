# 故事生成系统优化方案

> 请在每个方案后填写你的想法：✅ 采用 / ❌ 不采用 / ⏸️ 暂缓 / 💡 其他想法

---

## 一、温度策略优化

### 方案 A：统一降温

| 文件 | 当前值 | 建议值 |
|------|--------|--------|
| `story_generator.py:272` | 0.85 | 0.75 |
| `story_rewriter.py:96` | 1.0 | 0.75 |
| `option_generator.py:84` | 0.8 | 0.7 |

- **优点**：改动小，立即见效，减少幻觉
- **缺点**：可能降低创意性，故事风格趋同
- **难度**：⭐（5分钟）

**你的决定**：`_________做______________`

---

### 方案 B：动态温度 + 上下文感知

```python
# 根据故事类型调整温度
if has_pending_storylines or last_event_concluded == False:
    temperature = 0.65  # 需要延续剧情时更保守
else:
    temperature = 0.75  # 新事件可以更有创意
```

- **优点**：更智能，保持连贯性的同时允许创意
- **缺点**：逻辑复杂，需要更多测试
- **难度**：⭐⭐（30分钟）

**你的决定**：`_____________做__________`

---

## 二、提示词简化优化

### 方案 A：提取公共约束模块

将重复的约束提取为独立函数：

```python
def _build_common_constraints(language: str) -> str:
    """所有故事生成通用的约束"""
    # - 禁止跳脱叙事
    # - 禁止编造过往事件
    # - 人物名必须来自列表
    # - 时间逻辑一致性
    ...
```

- **优点**：减少重复，便于统一维护
- **缺点**：需要重构多个 prompt 函数
- **难度**：⭐⭐（1-2小时）

**你的决定**：`____________做___________`

---

### 方案 B：分层提示词架构

```
基础层：叙事风格、人称要求
约束层：人物限制、时间一致性
上下文层：历史故事、剧情线
```

- **优点**：结构清晰，易于调试
- **缺点**：改动大，需要重新设计
- **难度**：⭐⭐⭐（半天）

**你的决定**：`________做_______________`

---

## 三、一致性校验优化

### 方案 A：规则校验前置（推荐）

在 AI 校验前增加轻量级规则检查：

```python
def quick_validate(story: str, constraints: dict) -> list[str]:
    """快速规则校验，无需 AI 调用"""
    issues = []
    # 1. 检查人物名是否在允许列表
    # 2. 检查时间/地点是否与设定冲突
    # 3. 检查是否包含违禁词（"游戏"、"系统"等）
    return issues
```

- **优点**：减少 AI 调用，降低延迟 30-50%
- **缺点**：规则有限，无法检测复杂逻辑问题
- **难度**：⭐⭐（1-2小时）

**你的决定**：`__________先规则，后AI_____________`

---

### 方案 B：移除校验，强化提示词

将校验逻辑融入提示词，依赖模型自我约束。

- **优点**：零额外开销
- **缺点**：模型可能忽略约束
- **难度**：⭐（10分钟）

**你的决定**：`____________做___________`

---

### 方案 C：校验 + 自动修复

校验失败时，只修复问题段落而非重新生成整个故事。

- **优点**：保持故事连贯性，减少 token 消耗
- **缺点**：实现复杂，修复效果不稳定
- **难度**：⭐⭐⭐（半天）

**你的决定**：`__________不确定效果，先不做_____________`

---

## 四、选项生成优化

### 方案 A：增加 token 限制

```python
# option_generator.py:86
max_tokens=1000  →  max_tokens=2000
```

- **优点**：改动最小，防止截断
- **缺点**：可能还是不够
- **难度**：⭐（1分钟）

**你的决定**：`______________可以_________`

---

### 方案 B：选项一致性校验

在选项生成后，检查选项是否：
1. 与故事结尾的决策点匹配
2. 人物名在允许列表中
3. effects 数值合理

- **优点**：提高选项质量
- **缺点**：增加一次 AI 调用或规则检查
- **难度**：⭐⭐（1小时）

**你的决定**：`_______做________________`

---

## 五、流式输出优化

### 方案 A：重试时也支持流式

```python
# 当前：重试时不流式
cb = stream_callback if attempt == 0 else None

# 改进：重试时也流式，但先清空前端
if attempt > 0 and stream_callback:
    stream_callback("[RETRY]")  # 前端识别后清空
cb = stream_callback
```

- **优点**：用户体验一致
- **缺点**：需要前端配合
- **难度**：⭐⭐（前后端各30分钟）

**你的决定**：`_____________做__________`

---

### 方案 B：后台预生成 + 缓存

首次生成后缓存结果，重试时直接返回缓存。

- **优点**：重试秒返回
- **缺点**：需要缓存机制，可能返回旧内容
- **难度**：⭐⭐⭐（2小时）

**你的决定**：`___________不做____________`

---

## 六、历史上下文优化

### 方案 A：关键事实优先保留

```python
def _build_history_summary(...):
    # 1. 先提取关键事实（承诺、决策、状态变化）
    # 2. 再填充普通故事内容
    # 3. 确保关键事实不被截断
```

- **优点**：重要信息不丢失
- **缺点**：需要识别"关键事实"的逻辑
- **难度**：⭐⭐（1-2小时）

**你的决定**：`__________做_____________`

---

### 方案 B：向量检索相关历史

根据当前情境，只检索相关的历史片段。

- **优点**：上下文更精准
- **缺点**：需要向量数据库，增加复杂度
- **难度**：⭐⭐⭐（需要引入新依赖）

**你的决定**：`________做_______________`

---

## 七、其他想法

请在此填写你发现的其他问题或优化想法：

```
1. _______________________________________________________

2. _______________________________________________________

3. _______________________________________________________

4. _______________________________________________________

5. _______________________________________________________
```

---

## 最终决策汇总

| 序号 | 方案 | 决定 | 备注 |
|------|------|------|------|
| 一-A | 温度统一降温 | ✅ 采用 | |
| 一-B | 动态温度 | ✅ 采用 | |
| 二-A | 提取公共约束 | ✅ 采用 | |
| 二-B | 分层提示词 | ✅ 采用 | |
| 三-A | 规则校验前置 | ✅ 采用 | 先规则校验，后AI校验 |
| 三-B | 强化提示词约束 | ✅ 采用 | 保留AI校验 |
| 三-C | 校验+自动修复 | ⏸️ 暂缓 | 效果不确定 |
| 四-A | 增加token限制 | ✅ 采用 | |
| 四-B | 选项一致性校验 | ✅ 采用 | |
| 五-A | 重试流式输出 | ✅ 采用 | |
| 五-B | 缓存机制 | ❌ 不采用 | |
| 六-A | 关键事实优先 | ✅ 采用 | |
| 六-B | 向量检索 | ✅ 采用 | 最后实施，不影响现有流程 |

---

## 实施计划

### 第一期：快速见效（预计 1-2 小时）

| 优先级 | 方案 | 涉及文件 | 预计时间 |
|--------|------|----------|----------|
| 1 | 一-A 温度统一降温 | `story_generator.py`, `story_rewriter.py`, `option_generator.py` | 5分钟 |
| 2 | 四-A 增加token限制 | `option_generator.py` | 1分钟 |
| 3 | 一-B 动态温度 | `story_generator.py` | 30分钟 |

### 第二期：核心优化（预计 3-4 小时）

| 优先级 | 方案 | 涉及文件 | 预计时间 |
|--------|------|----------|----------|
| 4 | 三-A 规则校验前置 | 新建 `quick_validator.py`, 修改 `story_generator.py` | 1-2小时 |
| 5 | 四-B 选项一致性校验 | `option_generator.py` | 1小时 |
| 6 | 五-A 重试流式输出 | `story_generator.py`, 前端 SSE 处理 | 1小时 |
| 7 | 六-A 关键事实优先 | `summary_generator.py`, `_helpers.py` | 1小时 |

### 第三期：架构优化（预计 半天）

| 优先级 | 方案 | 涉及文件 | 预计时间 |
|--------|------|----------|----------|
| 8 | 二-A 提取公共约束 | `story_prompts.py`, `_helpers.py` | 1-2小时 |
| 9 | 二-B 分层提示词 | `story_prompts.py` | 半天 |
| 10 | 三-B 强化提示词约束 | `story_prompts.py` | 30分钟 |

### 第四期：进阶功能（待定）

| 优先级 | 方案 | 涉及文件 | 预计时间 |
|--------|------|----------|----------|
| 11 | 六-B 向量检索 | 新建 `vector_store.py`, 修改历史检索逻辑 | 需引入新依赖 |

---

## 实施原则

1. **渐进式改造**：每期完成后验证效果，再进行下一期
2. **向后兼容**：新功能通过开关控制，不影响现有流程
3. **可回滚**：每次改动后保留旧代码路径，出问题可快速回滚
4. **向量检索特殊处理**：第四期独立开发，通过配置开关启用，不影响现有流程