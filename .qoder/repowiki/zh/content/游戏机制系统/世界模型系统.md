# 世界模型系统

<cite>
**本文档引用的文件**
- [world_model.py](file://src/game/world_model.py)
- [world_model_updater.py](file://src/game/world_model_updater.py)
- [state.py](file://src/game/state.py)
- [story_generator.py](file://src/ai/story_generator.py)
- [consistency_validator.py](file://src/ai/consistency_validator.py)
- [story_analyzer.py](file://src/ai/story_analyzer.py)
- [profile_synthesizer.py](file://src/ai/profile_synthesizer.py)
- [models.py](file://src/database/models.py)
- [game_loop.py](file://src/game/game_loop.py)
</cite>

## 目录
1. [简介](#简介)
2. [项目结构](#项目结构)
3. [核心组件](#核心组件)
4. [架构概览](#架构概览)
5. [详细组件分析](#详细组件分析)
6. [依赖关系分析](#依赖关系分析)
7. [性能考虑](#性能考虑)
8. [故障排除指南](#故障排除指南)
9. [结论](#结论)
10. [附录](#附录)

## 简介

世界模型系统是本项目的核心数据管理机制，负责维护游戏世界的结构化数据状态。该系统通过统一的数据模型和约束机制，确保AI生成内容的一致性和叙事连贯性。

系统主要包含以下核心功能：
- **结构化数据管理**：角色位置、职业记录、承诺事项、因果链条等
- **AI交互机制**：与AI生成器协作，提供约束条件和验证服务
- **动态事实系统**：自动识别和管理世界状态变化
- **一致性保证**：通过约束文本和验证机制确保故事逻辑正确

## 项目结构

世界模型系统位于游戏模块的核心位置，采用分层架构设计：

```mermaid
graph TB
subgraph "世界模型层"
WM[WorldModel]
WU[WorldModelUpdater]
DS[数据结构]
end
subgraph "AI集成层"
SG[StoryGenerator]
CV[ConsistencyValidator]
SA[StoryAnalyzer]
PS[ProfileSynthesizer]
end
subgraph "状态管理层"
PSY[PlayerState]
CS[CharacterState]
end
subgraph "持久化层"
DB[(数据库)]
end
WM --> SG
WM --> CV
WU --> SA
WU --> PS
WM --> PSY
PSY --> DB
SG --> WM
CV --> WM
```

**图表来源**
- [world_model.py](file://src/game/world_model.py#L185-L641)
- [world_model_updater.py](file://src/game/world_model_updater.py#L14-L520)
- [story_generator.py](file://src/ai/story_generator.py#L24-L387)

**章节来源**
- [world_model.py](file://src/game/world_model.py#L1-L665)
- [world_model_updater.py](file://src/game/world_model_updater.py#L1-L520)
- [state.py](file://src/game/state.py#L244-L709)

## 核心组件

### 数据结构体系

世界模型系统定义了完整的数据结构层次：

```mermaid
classDiagram
class WorldModel {
+Dict~str,LocationInfo~ character_locations
+Dict~str,CareerInfo~ career_records
+Commitment[] active_commitments
+CausalChain[] causal_chains
+Dict~str,PhysicalState~ physical_states
+DynamicFact[] dynamic_facts
+Dict~str,CharacterProfile~ character_profiles
+int current_week
+str era
+from_player_state(player_state) WorldModel
+build_constraints_text(language) str
+check_geographic_feasibility(char_names) str[]
+check_career_plausibility(character, new_role, new_level) str[]
}
class LocationInfo {
+str location
+str region
+int since_week
+str travel_mode
+to_dict() Dict
+from_dict(d) LocationInfo
}
class CareerInfo {
+str current_job
+str employer
+str level
+int since_week
+Dict[] history
+to_dict() Dict
+from_dict(d) CareerInfo
}
class Commitment {
+str description
+str[] parties
+int deadline_week
+str status
+int created_week
+str importance
+to_dict() Dict
+from_dict(d) Commitment
}
class CausalChain {
+str cause
+str expected_consequence
+str[] characters
+int created_week
+bool resolved
+to_dict() Dict
+from_dict(d) CausalChain
}
class PhysicalState {
+str condition
+str severity
+int since_week
+int expected_recovery_week
+to_dict() Dict
+from_dict(d) PhysicalState
}
class CharacterProfile {
+str character
+str[] behavioral_traits
+str speech_style
+str[] decision_patterns
+str[] emotional_tendencies
+str[] behavioral_boundaries
+str constraint_text
+int evidence_count
+int last_updated_week
+to_dict() Dict
+from_dict(d) CharacterProfile
}
class DynamicFact {
+str fact_id
+str fact_type
+str subject
+str description
+str constraint_text
+str[] related_entities
+int source_week
+int expiry_week
+str importance
+bool active
+str supersedes
+to_dict() Dict
+from_dict(d) DynamicFact
}
WorldModel --> LocationInfo : "管理"
WorldModel --> CareerInfo : "管理"
WorldModel --> Commitment : "管理"
WorldModel --> CausalChain : "管理"
WorldModel --> PhysicalState : "管理"
WorldModel --> CharacterProfile : "管理"
WorldModel --> DynamicFact : "管理"
```

**图表来源**
- [world_model.py](file://src/game/world_model.py#L17-L161)

### 更新管理器

WorldModelUpdater负责处理各种数据更新操作：

```mermaid
flowchart TD
Start([开始更新]) --> CheckType{检查更新类型}
CheckType --> |位置更新| LocationUpdate[处理位置变更]
CheckType --> |职业更新| CareerUpdate[处理职业变更]
CheckType --> |承诺更新| CommitmentUpdate[处理承诺变更]
CheckType --> |因果链更新| CausalUpdate[处理因果链变更]
CheckType --> |AI分析| StoryAnalysis[运行故事分析器]
CheckType --> |角色画像| ProfileSynthesis[合成角色画像]
LocationUpdate --> RegionExtract[提取地区信息]
RegionExtract --> SaveLocation[保存位置数据]
CareerUpdate --> HistoryAppend[添加到历史记录]
HistoryAppend --> SaveCareer[保存职业数据]
CommitmentUpdate --> StatusUpdate[更新状态]
StatusUpdate --> Cleanup[清理过期数据]
CausalUpdate --> ResolutionCheck[检查解决状态]
ResolutionCheck --> Cleanup2[清理过期因果链]
StoryAnalysis --> NewFacts[生成新事实]
NewFacts --> Supersede[应用替代规则]
Supersede --> ExpireOld[过期旧事实]
ExpireOld --> LimitCount[限制数量]
ProfileSynthesis --> EvidenceCollect[收集行为证据]
EvidenceCollect --> Synthesize[合成画像]
Synthesize --> SaveProfile[保存角色画像]
SaveLocation --> End([结束])
SaveCareer --> End
Cleanup --> End
Cleanup2 --> End
LimitCount --> End
SaveProfile --> End
```

**图表来源**
- [world_model_updater.py](file://src/game/world_model_updater.py#L21-L520)

**章节来源**
- [world_model.py](file://src/game/world_model.py#L185-L641)
- [world_model_updater.py](file://src/game/world_model_updater.py#L14-L520)

## 架构概览

世界模型系统采用分层架构，实现了数据管理、AI集成和持久化的分离：

```mermaid
graph TB
subgraph "表现层"
UI[用户界面]
API[API接口]
end
subgraph "业务逻辑层"
GL[GameLoop]
WM[WorldModel]
WU[WorldModelUpdater]
end
subgraph "AI服务层"
SG[StoryGenerator]
CV[ConsistencyValidator]
SA[StoryAnalyzer]
PS[ProfileSynthesizer]
end
subgraph "数据访问层"
PSY[PlayerState]
DB[(数据库)]
end
UI --> GL
API --> GL
GL --> WM
GL --> WU
WM --> SG
WM --> CV
WU --> SA
WU --> PS
GL --> PSY
PSY --> DB
SG --> WM
CV --> WM
SA --> WM
PS --> WM
```

**图表来源**
- [game_loop.py](file://src/game/game_loop.py#L23-L1193)
- [story_generator.py](file://src/ai/story_generator.py#L24-L387)
- [consistency_validator.py](file://src/ai/consistency_validator.py#L42-L231)

## 详细组件分析

### 世界模型构建流程

WorldModel.from_player_state()方法负责从PlayerState构建完整的世界模型：

```mermaid
sequenceDiagram
participant GS as GameLoop
participant WM as WorldModel
participant PS as PlayerState
participant AI as AI服务
GS->>WM : from_player_state(player_state)
WM->>PS : 读取world_model_data
WM->>WM : 解析位置信息
WM->>WM : 解析职业记录
WM->>WM : 解析承诺事项
WM->>WM : 解析因果链条
WM->>WM : 解析身体状态
WM->>WM : 解析动态事实
WM->>WM : 解析角色画像
Note over WM : 处理遗留数据
WM->>PS : 读取established_facts
WM->>WM : 补充地理位置
WM->>WM : 补充职业信息
Note over WM : 初始化时代信息
WM->>PS : 读取era设置
WM->>WM : 设置era属性
GS-->>GS : 返回WorldModel实例
```

**图表来源**
- [world_model.py](file://src/game/world_model.py#L202-L287)

### 动态事实系统

动态事实系统是世界模型的核心创新，能够自动识别和管理世界状态变化：

```mermaid
flowchart TD
Start([故事生成完成]) --> ExtractFacts[AI分析故事]
ExtractFacts --> ParseResponse[解析AI响应]
ParseResponse --> CheckAction{检查动作类型}
CheckAction --> |new| CreateNew[创建新事实]
CheckAction --> |update| UpdateExisting[更新现有事实]
CheckAction --> |invalidate| MarkInactive[标记失效]
CreateNew --> SetImportance[设置重要性]
SetImportance --> SetExpiry[设置过期时间]
SetExpiry --> GenerateID[生成事实ID]
GenerateID --> AddToList[添加到列表]
UpdateExisting --> FindTarget[查找目标事实]
FindTarget --> CreateReplacement[创建替换事实]
CreateReplacement --> AddToList
MarkInactive --> SetFlag[设置inactive标志]
SetFlag --> RemoveFromList[从活动列表移除]
AddToList --> ApplySupersede[应用替代规则]
RemoveFromList --> ApplySupersede
ApplySupersede --> CheckExpiry[检查过期]
CheckExpiry --> LimitCount[限制数量]
LimitCount --> SaveToWM[保存到WorldModel]
SaveToWM --> End([完成])
```

**图表来源**
- [story_analyzer.py](file://src/ai/story_analyzer.py#L111-L317)
- [world_model_updater.py](file://src/game/world_model_updater.py#L262-L353)

### 一致性验证机制

ConsistencyValidator确保AI生成内容符合世界模型约束：

```mermaid
sequenceDiagram
participant SG as StoryGenerator
participant WM as WorldModel
participant CV as ConsistencyValidator
participant AI as AIClient
SG->>WM : build_constraints_text(language)
WM-->>SG : 返回约束文本
Note over SG : 生成故事后进行验证
SG->>CV : validate_story(story_text, world_model, ...)
CV->>WM : 获取约束文本
CV->>AI : 调用AI进行一致性检查
AI-->>CV : 返回验证结果
CV->>CV : 解析JSON响应
CV->>CV : 升级关键问题严重性
CV->>CV : 生成修复指令
alt 存在关键问题
CV-->>SG : 返回失败结果 + 修复指令
SG->>AI : 重新生成故事
AI-->>SG : 返回修正后的故事
else 无关键问题
CV-->>SG : 返回通过结果
end
```

**图表来源**
- [consistency_validator.py](file://src/ai/consistency_validator.py#L60-L231)
- [story_generator.py](file://src/ai/story_generator.py#L310-L374)

**章节来源**
- [world_model.py](file://src/game/world_model.py#L381-L627)
- [story_analyzer.py](file://src/ai/story_analyzer.py#L94-L317)
- [consistency_validator.py](file://src/ai/consistency_validator.py#L42-L231)

## 依赖关系分析

世界模型系统与其他模块的依赖关系如下：

```mermaid
graph TB
subgraph "核心依赖"
WM[WorldModel] --> State[PlayerState]
WM --> CV[ConsistencyValidator]
WM --> SA[StoryAnalyzer]
end
subgraph "更新依赖"
WU[WorldModelUpdater] --> WM
WU --> SA
WU --> PS[ProfileSynthesizer]
end
subgraph "生成依赖"
SG[StoryGenerator] --> WM
SG --> CV
end
subgraph "状态依赖"
State --> WM
State --> WU
end
subgraph "数据库依赖"
State --> DB[(数据库)]
DB --> State
end
subgraph "AI服务依赖"
WM --> AI[AIClient]
WU --> AI
SG --> AI
CV --> AI
SA --> AI
PS --> AI
end
```

**图表来源**
- [world_model.py](file://src/game/world_model.py#L1-L665)
- [world_model_updater.py](file://src/game/world_model_updater.py#L1-L520)
- [state.py](file://src/game/state.py#L1-L709)

**章节来源**
- [game_loop.py](file://src/game/game_loop.py#L1-L200)
- [models.py](file://src/database/models.py#L1-L171)

## 性能考虑

### 数据结构优化

1. **内存效率**
   - 使用dataclass减少内存开销
   - 字典存储支持快速查找和更新
   - 限制动态事实数量防止内存膨胀

2. **查询性能**
   - 基于字典的O(1)查找时间
   - 分类存储便于批量操作
   - 缓存常用查询结果

3. **序列化优化**
   - 自定义to_dict/from_dict方法
   - 支持增量更新减少IO开销
   - 压缩存储结构

### 并发处理

```mermaid
flowchart TD
Request[并发请求] --> Queue[请求队列]
Queue --> Process[串行处理]
Process --> Cache[缓存检查]
Cache --> |命中| Return[返回结果]
Cache --> |未命中| Compute[计算结果]
Compute --> Store[存储到缓存]
Store --> Return
subgraph "缓存策略"
TTL[时间戳过期]
SizeLimit[大小限制]
Priority[优先级淘汰]
end
Return --> Monitor[监控统计]
Monitor --> Request
```

## 故障排除指南

### 常见问题诊断

1. **世界模型构建失败**
   - 检查PlayerState的world_model_data格式
   - 验证数据完整性
   - 查看日志中的错误信息

2. **AI分析器异常**
   - 确认AI客户端配置正确
   - 检查网络连接状态
   - 验证prompt模板完整性

3. **一致性验证错误**
   - 检查约束文本生成
   - 验证AI响应格式
   - 查看具体问题描述

### 调试技巧

```python
# 启用详细日志
import logging
logging.basicConfig(level=logging.DEBUG)

# 检查世界模型状态
def debug_world_model_state(world_model):
    print(f"位置数量: {len(world_model.character_locations)}")
    print(f"职业记录: {len(world_model.career_records)}")
    print(f"承诺事项: {len(world_model.active_commitments)}")
    print(f"因果链条: {len(world_model.causal_chains)}")
    print(f"动态事实: {len(world_model.dynamic_facts)}")

# 验证特定约束
issues = world_model.check_geographic_feasibility(["张三", "李四"])
print(f"地理约束问题: {issues}")
```

**章节来源**
- [consistency_validator.py](file://src/ai/consistency_validator.py#L119-L231)
- [world_model_updater.py](file://src/game/world_model_updater.py#L351-L353)

## 结论

世界模型系统通过结构化的数据管理和智能的AI集成，为游戏提供了强大的叙事一致性保障。系统的主要优势包括：

1. **全面的数据覆盖**：涵盖地理位置、职业发展、承诺事项、因果关系等多个维度
2. **智能的动态管理**：通过AI自动识别和更新世界状态
3. **严格的约束保证**：确保生成内容符合逻辑和一致性要求
4. **良好的扩展性**：模块化设计支持功能扩展和性能优化

该系统为复杂世界状态的管理提供了可靠的解决方案，是实现高质量AI驱动叙事的关键基础设施。

## 附录

### 数据结构示例

以下是世界模型中各类数据结构的典型示例：

**位置信息示例**
```json
{
  "location": "北京市朝阳区",
  "region": "北京",
  "since_week": 12,
  "travel_mode": "resident"
}
```

**职业记录示例**
```json
{
  "current_job": "产品经理",
  "employer": "某科技公司",
  "level": "mid",
  "since_week": 8,
  "history": [
    {
      "job": "初级产品经理",
      "employer": "另一家公司",
      "level": "junior",
      "from_week": 0,
      "to_week": 8,
      "action": "promotion"
    }
  ]
}
```

**承诺事项示例**
```json
{
  "description": "答应周末陪妈妈去医院",
  "parties": ["主角", "母亲"],
  "deadline_week": 15,
  "status": "pending",
  "created_week": 12,
  "importance": "normal"
}
```

**因果链条示例**
```json
{
  "cause": "得罪了部门主管李总",
  "expected_consequence": "可能影响晋升评审",
  "characters": ["张三"],
  "created_week": 10,
  "resolved": false
}
```

**动态事实示例**
```json
{
  "fact_id": "f_zhangwei_injury_w12",
  "fact_type": "physical_state",
  "subject": "张伟",
  "description": "右臂骨折",
  "constraint_text": "张伟右臂打着石膏，不能提重物或做剧烈运动",
  "related_entities": ["张伟"],
  "source_week": 12,
  "expiry_week": 20,
  "importance": "important",
  "active": true,
  "supersedes": ""
}
```

### 查询优化建议

1. **索引策略**
   - 为常用查询字段建立索引
   - 使用复合索引优化多条件查询
   - 定期分析查询性能

2. **缓存策略**
   - 缓存热点数据
   - 实施适当的过期策略
   - 监控缓存命中率

3. **批处理优化**
   - 合并频繁的小更新
   - 使用事务减少锁竞争
   - 异步处理耗时操作

4. **内存管理**
   - 定期清理过期数据
   - 实施内存使用监控
   - 优化数据结构存储